import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mostLikeYou: {
    flexDirection: "row",
  },
  communityList: {
    flexDirection: "row",
    flexWrap: "wrap",
  },
});

export default styles;
