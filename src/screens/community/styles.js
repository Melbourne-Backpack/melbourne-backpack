import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mostLikeYou: {
    flexDirection: "row",
    overflow: "hidden",
  },
  communityList: {
    flexDirection: "row",
    flexWrap: "wrap",
  },
});

export default styles;
